Prerequisites: 

node ^18.13.0 || ^20.9.0
Angular CLI version 17.0.x 

npm install to install node-modules specified in package.json

navigate to the backend folder and start with 
npm run start

surve to {url}/init to fill the database